"""Default paths and application settings."""

from pathlib import Path

DEFAULT_SCAN_PATH = Path(r"C:\DeepArt")
APP_NAME = "CubaseTools"
APP_VERSION = "1.0.0"
WINDOW_WIDTH = 1200
WINDOW_HEIGHT = 800
WINDOW_MIN_WIDTH = 900
WINDOW_MIN_HEIGHT = 600
